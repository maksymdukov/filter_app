// node modules
import React from 'react';

// styles
import './styles.scss';

const Loading = () => {
    return (
        <div className="loading">
            <span className="dot1"/>
            <span className="dot2"/>
            <span className="dot3"/>
        </div>
    );
};

export default Loading;